# ── 시크릿 (절대 커밋 금지) ──────────────────────────
.env
.env.*
!.env.example
*.key
*.pem

# ── 실행 상태 / 기록 (계정별 데이터) ─────────────────
state.json
state.json.bak
engine_state.json
governor_state.json
trades.jsonl
proposals/
*_backup_*.json
_archive_*/

# ── 로그 / PID ───────────────────────────────────────
*.log
*.log.*
*.pid

# ── 파이썬 ───────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/

# ── 에디터 / 로컬 도구 ───────────────────────────────
.claude/
.vscode/
.idea/
*.bak_*
